<%= form_for(show) do |f| %>
  <% if show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show.errors.count, "error") %> prohibited this show from being saved:</h2>

      <ul>
      <% show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %>
    <%= f.number_field :movie_id %>
  </div>

  <div class="field">
    <%= f.label :auditorium_id %>
    <%= f.number_field :auditorium_id %>
  </div>

  <div class="field">
    <%= f.label :start_datetime %>
    <%= f.datetime_select :start_datetime %>
  </div>

  <div class="field">
    <%= f.label :end_datetime %>
    <%= f.datetime_select :end_datetime %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
